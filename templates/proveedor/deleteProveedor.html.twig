{% extends 'base.html.twig' %}

{% block content %}
<br>
<div class="container-fluid">
    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <h4 class="text-center">Borrar un proveedores</h4>
            <div class="col-6 bg-gradient rounded my-3 p-5 te">
                {{ form_start(form) }}
                <div class="row text-center">
                    <h6>Quieres eliminar el proveedor con nombre: {{ name }}</h6>
                </div>
                <br>
                <div class="row text-center align-items-center justify-content-center">
                    <div class="col-6">
                        <div class="row">
                            <div class="col">
                                {{ form_row(form.Borrar) }}
                            </div>
                        </div>

                    </div>

                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
    {% endblock %}
